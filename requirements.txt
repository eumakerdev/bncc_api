# ---------------------------------------------------------------------------
# Plataforma Pública da BNCC API — dependências canônicas (ver CLAUDE.md/plan.md)
# ---------------------------------------------------------------------------

# Web framework / API
fastapi==0.141.1  # >=0.115.3 corrige CVE-2024-47874; >=0.130 exige starlette/pydantic abaixo
uvicorn[standard]==0.52.4
starlette==1.6.0  # >=1.0.1 corrige CVE-2026-48710 (BadHost: bypass de auth via Host header); pin explícito
pydantic==2.13.4  # exigido por fastapi>=0.130 (>=2.9.0)
pydantic-settings==2.15.0  # acompanha o bump de pydantic (exige pydantic>=2.7.0)
python-multipart==0.0.32  # >=0.0.7 corrige CVE-2024-24762 (ReDoS no parsing de boundary multipart)

# Plataforma: banco relacional (SQLAlchemy 2.0 async + Alembic)
# SQLite (aiosqlite) em dev; Postgres (asyncpg) em produção / Cloud SQL.
SQLAlchemy==2.0.52
alembic==1.19.1
aiosqlite==0.22.1
asyncpg==0.31.0
greenlet==3.5.5

# Segurança: hash de senha (Argon2), JWT de sessão do portal
passlib[argon2]==1.7.4
argon2-cffi==25.1.0
PyJWT==2.13.0
email-validator==2.3.0

# Cliente HTTP async — chamadas OAuth (login social) aos provedores em runtime.
# Também usado pela suíte de testes (ASGITransport).
httpx==0.28.1

# E-mail (verificação de conta) — SMTP async; backend console em dev
aiosmtplib==5.1.2

# Rate limiting
slowapi==0.1.10

# Transparência de custos — leitura do BigQuery billing export (só no job de
# ingestão `scripts/ingest_costs.py`; import lazy, o app web nunca o carrega).
# Cliente CORE (sem extras pandas/bqstorage): superfície mínima de dependências.
google-cloud-bigquery==3.43.0

# Superfícies visuais (SSR: landing + portal + docs)
Jinja2==3.1.6

# Vetores / IA (RAG) — camada aumentada e opcional. LangChain foi REMOVIDO da
# stack canônica (Constituição v1.1.0): nunca foi importado no código e seus pinos
# legados travavam numpy<2 e packaging<24 na resolução de dependências.
#
# Com o LangChain fora, os pinos anti-backtracking `build==1.3.0`/`packaging==23.2`
# deixaram de ser necessários: eles só existiam porque `langchain-core` 0.1.x exigia
# `packaging<24.0` enquanto `chromadb` puxava `build>=1.4.0` (que exige
# `packaging>=24.0`) — o pip descia versão a versão de `build`, re-resolvendo a
# árvore inteira a cada tentativa (~175s por iteração). Sem o conflito, a resolução
# converge direto e o pip escolhe as versões mais novas.
chromadb==1.5.9
sentence-transformers==5.7.0  # 2.2.2 quebra com huggingface_hub>=0.26 (cached_download removido)
openai==2.53.0
google-generativeai==0.8.6

# numpy: dependência de RUNTIME (chromadb/sentence-transformers operam sobre
# ndarray; `vector_store` chama `.tolist()` nos embeddings). O teto `<2` vinha do
# langchain 0.1.0; com ele fora da stack o pino 1.x é apenas conservador — o bump
# para numpy 2.x fica liberado para o Dependabot propor, validado pelos portões de CI.
# 1.26.4 (último da linha 1.x) destrava pandas 3.x nos scripts de extração (dev),
# que exige numpy>=1.26.
numpy==1.26.4
requests==2.34.2

# Produção
gunicorn==26.1.0
python-dotenv==1.2.3

# ---------------------------------------------------------------------------
# NÃO adicione aqui ferramentas de teste/lint/docs nem a stack de extração de
# PDF: nada em `app/` as importa, e tudo neste arquivo é instalado na imagem de
# produção. Elas vivem em `requirements-dev.txt`. Ver o comentário no Dockerfile.
# ---------------------------------------------------------------------------
