# ---------------------------------------------------------------------------
# Plataforma Pública da BNCC API — dependências canônicas (ver CLAUDE.md/plan.md)
# ---------------------------------------------------------------------------

# Web framework / API
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
pydantic-settings==2.1.0
python-multipart==0.0.6

# Plataforma: banco relacional (SQLAlchemy 2.0 async + Alembic + SQLite driver)
SQLAlchemy==2.0.25
alembic==1.13.1
aiosqlite==0.19.0
greenlet==3.0.3

# Segurança: hash de senha (Argon2), JWT de sessão do portal
passlib[argon2]==1.7.4
argon2-cffi==23.1.0
PyJWT==2.8.0
email-validator==2.1.0

# E-mail (verificação de conta) — SMTP async; backend console em dev
aiosmtplib==3.0.1

# Rate limiting
slowapi==0.1.9

# Superfícies visuais (SSR: landing + portal + docs)
Jinja2==3.1.3

# Vetores / IA (RAG) — camada aumentada e opcional
chromadb==0.4.18
langchain==0.1.0
langchain-community==0.0.10
sentence-transformers==2.2.2
openai==1.3.8
google-generativeai==0.3.2

# Processamento de dados / extração determinística de PDF
pandas==2.1.4
numpy==1.24.3
requests==2.31.0
beautifulsoup4==4.12.2
lxml==4.9.3
pdfplumber==0.10.3
PyPDF2==3.0.1
pypdf==6.14.2
pikepdf==10.9.1

# Testes + cobertura (gate de CI ≥ 80%)
pytest==7.4.3
pytest-cov==4.1.0
pytest-asyncio==0.21.1
httpx==0.25.2

# Qualidade de código
ruff==0.1.6
black==23.11.0
mypy==1.7.1
pre-commit==3.6.0

# Documentação
mkdocs==1.5.3
mkdocs-material==9.4.8

# Produção
gunicorn==21.2.0
python-dotenv==1.0.0
